# Visual Effects SDK

This folder contains the public BGL visual effects SDK entry points, samples and manifest examples. The ABI header lives in `src/extensions/bgl-plugin-api.h`; the main guide is `docs/visual-effects-sdk.md`.
