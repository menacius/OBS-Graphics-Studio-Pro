# Rendering Engine

Target home for GPU/OBS rendering, Cairo legacy paths, textures, alpha handling,
blending, filters, render caches, invalidation, and performance-critical drawing.
Keep public APIs backend-oriented and independent from editor widgets.
