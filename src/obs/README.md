# OBS Integration

Owns OBS plugin registration, source lifecycle, source properties, preview/live
output, OBS-specific rendering entry points, and reuse of OBS filters or shaders.
Editor-only logic should stay outside this module.
